{% for argument in function.getArguments %}
    <li>
        <h3>{{ argument.name }}</h3>
        {% if (language == "en") %}
            <p>{{ argument.getEnglishDescription }}</p>
        {% else %}
            <p>{{ argument.getFrenchDescription }}</p>
        {% endif %}
    </li>
{% endfor %}
