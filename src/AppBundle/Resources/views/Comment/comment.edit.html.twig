{% extends 'AppBundle::layout.html.twig' %}


{% block title %}{{ 'sitename' | trans }} | {{ 'CreateComment.title'|trans }}{% endblock %}

{% block content %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="{{ asset('bundles/app/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css') }}" />
{% endblock stylesheets %}
   
{% block javascripts_body %}
    {{ parent() }}
    <script type="text/javascript" charset="utf-8" src="{{ asset('bundles/app/bootstrap-datepicker/dist/js/bootstrap-datepicker.min.js') }}"></script>
{% endblock javascripts_body %}

<div class="container">
    <div class="row">
        <div class="col-md-push-2 col-md-8 white_background boxElement">
            <h2 class="main_title">{{'navbar.admin.comment.edit' | trans}} <span class="glyphicon glyphicon-pushpin" aria-hidden="true"></span></h2>
                 
                 <p class="bg-warning"> 
                    {{ form_errors(form) }}
                 </p>
                 
                 {% set tt = path('app_admin_commentEdit', {'id': app.request.get('id') } )  %}

                 {{ form_start(form,{'action': path('app_admin_commentEdit', {'id': app.request.get('id')  } ) } ) }}
                    
                    {{ form_widget(form._token) }}
                    
                    <div class="form-group">
                        {{ form_row(form.user) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.comment) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.postedAt) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.save) }}
                    </div>
                {{ form_end(form) }} 
    </div>
</div>
</div>
{% endblock content %}
